# 2026 AI·디지털 활용 선도학교 운영 길잡이 기능 명세

## 1. 문서 목적

이 문서는 `prd.md`의 사용자 요구를 화면 동작, 데이터 정의, 집계 규칙, 공개 범위와 합격 조건으로 구체화한다. 구현 방식보다 결과의 정확한 동작을 정의한다.

## 2. 데이터 범위

| 데이터 영역 | 기준일 | 공개 단위 | 핵심 용도 |
|---|---|---|---|
| 예산 집행 | 2026-07-01 | 익명 전체 분포·평균 | 네 예산 영역과 우리 학교 비교 |
| 과제 사례 | 2026년 1학기 | 익명 사례 카드 | 필수·선택과제의 운영 흐름 |
| 확장 에듀테크 | 2026-07-24 | 전체·초·중·고 집계 | 구매·구독 확인 빈도와 학교급 비교 |

확장 에듀테크 데이터의 학교급별 모집단은 다음과 같다.

- 전체: 177개교
- 초등: 89개교
- 중등: 51개교
- 고등: 37개교

이 숫자는 집계 검증과 비율 계산에 사용한다. 기본 화면의 주요 메시지로 강조하지 않는다.

## 3. 핵심 데이터 정의

### 3.1 학교급

```ts
type SchoolLevel = "all" | "elementary" | "middle" | "high";
```

표시명은 각각 `전체`, `초등`, `중등`, `고등`이다.

### 3.2 도구 집계

```ts
type EdtechAggregate = {
  id: string;
  name: string;
  group: EdtechGroup;
  purpose: string;
  counts: {
    all: number;
    elementary: number;
    middle: number;
    high: number;
  };
  rates: {
    all: number;
    elementary: number;
    middle: number;
    high: number;
  };
  ranks: {
    all: number;
    elementary: number;
    middle: number;
    high: number;
  };
  relatedToolIds: string[];
  insights: Record<SchoolLevel, ToolInsight>;
};
```

```ts
type ToolInsight = {
  headline: string;
  interpretation: string;
  comparison: string;
  semesterQuestion: string;
  actions: string[];
  limitation: string;
};
```

### 3.3 기능 유형

```ts
type EdtechGroup =
  | "협업·공유"
  | "언어·문해"
  | "퀴즈·참여"
  | "교과·맞춤형"
  | "생성형 AI"
  | "콘텐츠 제작"
  | "학습관리"
  | "코딩·컴퓨팅";
```

화면에서는 도구당 대표 유형 하나를 사용한다. 여러 기능을 가진 도구는 상세 설명에서 보조 기능을 안내할 수 있다.

### 3.4 교사개발 웹앱

```ts
type TeacherWebAppSummary = {
  total: 24;
  byLevel: {
    elementary: 8;
    middle: 12;
    high: 4;
  };
  purposeGroups: TeacherWebAppPurpose[];
  examples: AnonymousTeacherWebAppExample[];
  insight: Record<SchoolLevel, ToolInsight>;
};
```

포함 범위:

- 교사가 생성형 AI를 활용해 만든 앱
- 웹사이트와 대시보드
- 챗봇과 플랫폼
- 시뮬레이터
- 수업·평가·피드백·학급운영·업무지원 도구

제외 범위:

- 학생 제작 중심 앱
- 교사가 개발하지 않고 기존 도구를 사용하기만 한 사례

혼합 사례에서는 교사가 개발한 부분만 포함하고 학생 제작 부분은 제외한다.

## 4. 집계 규칙

### 4.1 도구명 정규화

- 대소문자, 띄어쓰기, 한글·영문 표기 차이를 대표 이름으로 통합한다.
- 같은 제품의 세부 요금제는 기능이 같으면 하나의 도구로 합친다.
- 서로 다른 제품은 이름이 유사해도 합치지 않는다.
- 별칭 변경은 별도 사전에서 관리한다.

### 4.2 학교 단위 중복 제거

한 학교에서 같은 도구가 여러 번 등장하면 해당 도구에 대해 한 번만 센다.

```text
도구 선택 학교 수 = 선택한 학교급에서 해당 도구가 한 번 이상 확인된 고유 학교 수
```

### 4.3 비율

```text
도구 비율 = 도구 선택 학교 수 ÷ 선택한 학교급의 전체 학교 수 × 100
```

- 소수점 첫째 자리까지 표시한다.
- 전체 분모는 177, 초등은 89, 중등은 51, 고등은 37이다.
- 한 학교가 여러 도구에 포함될 수 있으므로 도구별 비율의 합은 100%가 아닐 수 있다.

### 4.4 순위

- 선택한 학교급의 고유 학교 수 내림차순으로 정렬한다.
- 학교 수가 같으면 대표 도구명 가나다·알파벳 순으로 정렬해 화면이 매번 바뀌지 않게 한다.
- 순위는 선택한 학교급이 바뀔 때 다시 계산한다.

### 4.5 관련 도구

- 동일 학교에서 함께 확인된 횟수가 많은 순으로 최대 5개를 제시한다.
- 함께 등장했다는 사실만 나타내며, 함께 사용했거나 효과가 있다는 의미로 표현하지 않는다.
- 교사개발 웹앱은 상용 도구의 관련 도구 목록에서도 제외한다.

### 4.6 교사개발 웹앱 분리

- 상용 에듀테크 워드클라우드와 순위에 포함하지 않는다.
- 별도 카드와 상세 대화상자에서만 집계한다.
- 확정 집계값은 전체 24건, 초등 8건, 중등 12건, 고등 4건이다.
- 학생 제작 중심 1건은 전체 제외한다.
- 혼합형 2건은 학생 제작 설명을 제외하고 교사개발 부분만 포함한다.

## 5. 에듀테크 화면 명세

### 5.1 화면 머리말

- 제목: `어떤 에듀테크를 많이 선택했을까요?`
- 보조 문구: 구매·구독 빈도를 학교급별로 살펴볼 수 있다는 한 문장
- 기준 표시: `7.24 기준`
- 학교급 선택 버튼: 전체·초등·중등·고등

원자료 개수, 저장소 커밋, 정제 방법, 제외 사례 설명은 머리말에 표시하지 않는다.

### 5.2 학교급 선택 버튼

- 한 번에 하나만 선택한다.
- 기본 선택은 전체다.
- 선택값은 워드클라우드, 순위, 비율, 상세 패널, 활용 유형, 인사이트에 공통 적용한다.
- Tab과 Enter 또는 Space로 조작할 수 있어야 한다.
- 학교급 선택은 현재 화면에서 유지하며 서버에 저장하지 않는다.

### 5.3 요약 영역

큰 숫자를 여러 개 나열하지 않는다. 다음 두 내용을 문장형 카드로 제공한다.

- 선택 학교급에서 가장 많이 확인된 도구 유형
- 다른 학교급과 비교해 두드러진 선택 경향

요약 카드에는 근거 비율과 해석을 함께 표시하고, 도구별 근거는 인접 상세 영역에서 확인한다.

### 5.4 워드클라우드

- 모든 도구는 검색 또는 페이지 이동으로 접근 가능해야 한다.
- 글자 크기는 선택 학교 수를 기준으로 최소 18px, 최대 64px 사이에서 계산한다.
- 로그 스케일 또는 제곱근 스케일을 사용해 1위만 지나치게 커지지 않게 한다.
- 빈도 1건인 도구도 읽을 수 있는 크기를 유지한다.
- 회전은 -6도, 0도, +6도 범위만 허용한다.
- 글자 간 최소 여백을 보장한다.
- 컨테이너 경계를 넘어가는 단어가 없어야 한다.
- 도구명은 중간에서 줄바꿈하지 않는다.
- 선택된 도구 옆에 해당 학교급 비율을 표시한다.

### 5.5 검색과 기능 필터

- 도구명과 핵심 기능을 검색한다.
- 기능 유형 필터는 기존 8개 유형과 `전체`를 제공한다.
- 검색어와 유형 필터를 함께 적용할 수 있다.
- 결과가 없으면 오류가 아닌 안내 문장을 표시한다.
- 필터를 바꿔도 현재 학교급 선택은 유지한다.

### 5.6 도구 상세 패널

데스크톱:

- 워드클라우드 오른쪽에 고정된 상세 패널로 표시한다.
- 페이지 맨 아래로 스크롤시키지 않는다.

모바일:

- 워드클라우드 바로 다음에 상세 패널을 배치한다.
- 선택한 도구가 바뀌면 같은 상세 패널 내용이 즉시 갱신된다.
- 필수 정보를 마우스 올림 상태에만 숨기지 않는다.

필수 항목:

1. 도구명과 대표 기능 유형
2. 선택 학교급의 순위
3. 고유 학교 수와 비율
4. 초·중·고 비교 막대
5. 실제 핵심 기능
6. 활용 목적에 대한 해석
7. 관련 도구
8. 2학기 점검 질문
9. 구체적인 다음 행동
10. 구매 빈도만으로 효과를 판단할 수 없다는 한계
11. `사용 전 이용약관을 꼭 확인하세요`
12. 서울시교육청 가이드라인 링크

### 5.7 교사개발 웹앱 카드

- 워드클라우드와 상용 도구 순위 사이에 독립 카드로 둔다.
- 명칭은 항상 `교사개발 웹앱`으로 쓴다.
- 선택한 학교급에 맞는 건수와 주요 개발 목적을 보여준다.
- 카드를 선택하면 익명 예시와 상세 인사이트를 대화상자로 연다.
- 학생 제작 앱을 별도 카드나 수치로 표시하지 않는다.
- 학교명과 제외 규칙 설명을 화면에 표시하지 않는다.

### 5.8 정확한 순위 목록

- 워드클라우드 아래 접을 수 있는 표로 제공한다.
- 선택한 학교급과 검색·기능 필터를 그대로 적용한다.
- 순위, 도구명, 고유 학교 수, 비율, 대표 유형, 핵심 기능을 표시한다.
- 표에서 도구명을 선택하면 같은 상세 패널을 연다.
- 상위 10개 이후의 모든 도구도 포함한다.

## 6. 인사이트 작성 규칙

각 인사이트는 다음 순서를 따른다.

1. `관찰`: 데이터에서 직접 확인되는 내용
2. `해석`: 가능한 운영 맥락
3. `비교`: 다른 학교급과의 차이
4. `2학기 질문`: 학교가 확인할 사안
5. `한계`: 데이터만으로 단정할 수 없는 내용

인사이트는 `많다`, `적다`, `상대적으로 높다`의 근거 수치를 가져야 한다. 구매 빈도에서 교육 효과, 만족도, 지속 사용 의향을 추론하지 않는다.

## 7. 문구와 표시 규칙

- `AI선도학교`가 아니라 `AI·디지털 활용 선도학교`로 쓴다.
- `구매·구독이 확인된 학교 비율`을 기본 표현으로 사용한다.
- `전체 총액`, `미집행 학교`, `부진`을 핵심 메시지로 사용하지 않는다.
- 도구 상세에는 어려운 용어보다 실제 수업 기능을 먼저 쓴다.
- 화면에 개발 방식과 내부 분류 이유를 설명하지 않는다.
- 도구명 아래 또는 상세 패널에 `사용 전 이용약관을 꼭 확인하세요`를 표시한다.
- 학생 사용 관련 외부 자료는 서울시교육청 링크만 유지한다.

## 8. 개인정보 명세

- 공개 데이터 구조에는 학교명 필드를 두지 않는다.
- 도구 상세와 사례 예시에 학교명이나 학교를 추정할 수 있는 고유 표현을 넣지 않는다.
- 원자료는 `public` 디렉터리와 브라우저 번들에 포함하지 않는다.
- 학교명 목록을 기준으로 빌드 결과물을 검사해 한 건이라도 발견되면 배포를 중단한다.
- 브라우저에 입력한 우리 학교 수치는 네트워크로 전송하지 않는다.

## 9. 접근성과 반응형

- 모든 버튼의 최소 터치 영역은 44×44px이다.
- 색상 외에 텍스트와 형태로 유형과 선택 상태를 구분한다.
- 워드클라우드 항목은 버튼으로 구현하고 읽을 수 있는 접근성 이름을 제공한다.
- 툴팁에만 필수 정보를 숨기지 않는다.
- 키보드와 터치로 상세 패널을 열고 닫을 수 있어야 한다.
- 모션 감소 설정에서는 재배치와 전환 애니메이션을 최소화한다.
- 본문과 상세 분석 글자는 현재 웹앱의 확대된 가독성 기준을 유지한다.
- 360px, 768px, 1280px, 1440px 너비에서 단어 잘림과 카드 겹침이 없어야 한다.

## 10. 합격 조건

| 구분 | 합격 조건 |
|---|---|
| 기준일 | 에듀테크 화면에 `7.24 기준` 표시 |
| 학교급 | 전체·초·중·고 선택에 따라 모든 관련 수치가 함께 변경 |
| 모집단 | 177 = 89 + 51 + 37 검증 |
| 중복 제거 | 같은 학교·같은 도구는 한 번만 집계 |
| 교사개발 웹앱 | 전체 24, 초 8, 중 12, 고 4 |
| 학생 제작 | 학생 제작 중심 사례가 공개 집계와 예시에 없음 |
| 혼합 사례 | 교사개발 부분만 포함 |
| 워드클라우드 | 모든 도구 접근 가능, 글자 크기 차등, 잘림·겹침 없음 |
| 상세 분석 | 클릭 즉시 인접 패널 또는 대화상자에서 확인 |
| 학교명 | HTML, JS, 접근성 문구와 공개 데이터에서 0건 |
| 문구 | 효과·만족도·선호를 구매 빈도만으로 단정하지 않음 |
| 안전 안내 | 이용 가능 연령·학교 도입 절차·개인정보 확인과 서울시교육청 링크 제공 |
| 기존 기능 | 예산·우리 학교 비교·과제 사례 탭이 정상 동작 |

# 2026 AI·디지털 활용 선도학교 운영 길잡이 구현 계획

## 1. 구현 목표

현재 Next.js 웹앱의 디자인과 예산·과제 사례 기능을 유지하면서, 에듀테크 데이터를 2026년 7월 24일 기준 초·중·고 자료로 교체한다. 학교급별 구매·구독 빈도와 인사이트를 제공하고, 교사가 생성형 AI로 만든 앱과 도구는 `교사개발 웹앱`으로 분리한다.

이번 작업은 기존 사이트를 새로 만드는 것이 아니라 에듀테크 탭의 데이터 구조와 상호작용을 확장하는 작업이다.

## 2. 현재 구조와 변경 방향

현재 주요 파일:

- `app/page.tsx`: 화면, 에듀테크 데이터, 상태와 상호작용이 함께 있음
- `app/globals.css`: 전체 디자인과 반응형 스타일
- `app/case-data.ts`: 과제별 정성 분석
- `app/case-catalog-data.ts`: 익명 사례 카드
- `app/layout.tsx`: 사이트 제목과 공유 메타데이터
- `tests/rendered-html.test.mjs`: 렌더링 결과 검증

변경 후에는 에듀테크 데이터와 집계 결과를 화면 코드에서 분리한다.

```text
app/
├─ page.tsx
├─ globals.css
├─ edtech-data.ts
├─ edtech-types.ts
├─ case-data.ts
└─ case-catalog-data.ts
scripts/
└─ build-edtech-data.mjs
tests/
├─ rendered-html.test.mjs
└─ edtech-data.test.mjs
```

원자료는 브라우저가 내려받는 `public` 폴더에 넣지 않는다. 공개 코드에는 학교별 기록 대신 학교급별 익명 집계만 포함한다.

## 3. 데이터 준비

### 3.1 기준 스냅샷 고정

- 기준 시점: 2026-07-24 23:59 KST
- 기준 소스 버전: `9819ddad0288e65279803c8523d37fdc0ea0b0d4`
- 예상 학교 수: 전체 174, 초등 86, 중등 51, 고등 37

실제 Vercel 빌드에서 외부 사이트를 실시간 호출하지 않는다. 개발 단계에서 기준 버전의 데이터를 읽고 익명 집계 파일을 생성한 뒤, 생성된 집계 데이터만 배포한다.

### 3.2 도구명 정규화

`scripts/build-edtech-data.mjs`의 제품 목록과 별칭 배열에서 다음 내용을 관리한다.

- 영문 대소문자 차이
- 한글·영문 표기 차이
- 띄어쓰기 차이
- 제품명과 요금제 표기 차이
- 통합하면 안 되는 유사 제품

정규화 결과에 새 도구명이 남으면 자동으로 합치지 않고 검토 목록으로 보낸다.

### 3.3 고유 학교 집계

집계 과정:

1. 학교급을 초등·중등·고등으로 표준화한다.
2. 도구명을 대표 이름으로 바꾼다.
3. 학교×도구 중복을 제거한다.
4. 학교급별 고유 학교 수를 계산한다.
5. 전체·초·중·고 비율과 순위를 계산한다.
6. 같은 학교에서 함께 나타난 도구를 집계한다.
7. 상용 도구 결과에서 교사개발 웹앱을 제거한다.
8. 학교명을 버린 익명 집계 파일을 생성한다.

```ts
const denominators = {
  all: 174,
  elementary: 86,
  middle: 51,
  high: 37,
};

rate = round1(uniqueSchoolCount / denominators[level] * 100);
```

### 3.4 교사개발 웹앱 분류

앱, 웹사이트, 대시보드, 챗봇, 시뮬레이터, 수업·평가·업무 도구 제작을 모두 `교사개발 웹앱`으로 통합한다.

검증값:

```ts
const teacherWebApps = {
  all: 24,
  elementary: 8,
  middle: 12,
  high: 4,
};
```

처리 원칙:

- 학생 제작 중심 사례 1건은 전체 제외
- 교사와 학생이 함께 제작한 혼합 사례 2건은 교사 제작 부분만 유지
- 학교명과 학생 제작 문장은 생성 결과에 포함하지 않음
- 상용 도구 순위와 워드클라우드에서 제외

자동 키워드만으로 분류하지 않고, 이번 기준 스냅샷의 24건은 수동 검토된 확정 집계로 관리한다. 이후 데이터 갱신 시 새 후보만 다시 검토한다.

### 3.5 인사이트 데이터

도구와 학교급마다 다음 구조를 준비한다.

```ts
type ToolInsight = {
  headline: string;
  observation: string;
  interpretation: string;
  comparison: string;
  semesterQuestion: string;
  actions: string[];
  limitation: string;
};
```

인사이트는 계산 가능한 관찰값과 교사에게 필요한 해석을 분리한다. 학교급 차이가 작으면 억지로 차이를 만들지 않고 `학교급 간 큰 차이가 확인되지 않았습니다`라고 표현한다.

## 4. 화면 구현

### 4.1 에듀테크 데이터 분리

`page.tsx`에 직접 작성된 `edutech`, `toolPurposes`, `functionGroups` 데이터를 `app/edtech-data.ts`로 이동한다.

화면은 데이터 배열을 직접 수정하지 않고 선택한 학교급에 맞는 계산 결과만 읽는다.

### 4.2 상태 구조

```ts
type EdtechViewState = {
  level: SchoolLevel;
  group: EdtechGroup | "all";
  query: string;
  page: number;
  selectedToolId: string | null;
  teacherWebAppOpen: boolean;
};
```

학교급 변경 시:

- 필터와 검색어는 유지
- 페이지는 첫 페이지로 이동
- 현재 선택 도구가 새 조건에 없으면 새 순위의 첫 도구를 선택
- 도구 상세, 워드클라우드, 순위표, 인사이트를 한 번에 갱신

### 4.3 학교급 선택기

에듀테크 섹션 제목 아래에 세그먼트 버튼을 추가한다.

```text
[전체] [초등] [중등] [고등]    7.24 기준
```

데이터 규모나 정제 과정에 대한 설명은 넣지 않는다. 선택 상태는 색상, 굵기와 `aria-selected`로 함께 표현한다.

### 4.4 워드클라우드

기존 버튼 기반 접근성 구조를 유지하고 크기 계산을 연속 함수로 교체한다.

```ts
const wordSize = minSize +
  (Math.sqrt(count) - Math.sqrt(minCount)) /
  (Math.sqrt(maxCount) - Math.sqrt(minCount)) *
  (maxSize - minSize);
```

- 데스크톱: 18~64px
- 모바일: 16~42px
- 회전: -6·0·+6도
- 빈도와 무관한 고정 크기 구간을 제거
- 단어 높이와 너비를 고려해 행 배치
- 카드의 `overflow: hidden` 때문에 잘리지 않도록 내부 여백 확보
- 한 페이지 항목 수는 24개로 제한

모든 도구는 페이지 이동, 검색 또는 정확한 순위표에서 접근할 수 있어야 한다.

### 4.5 상세 패널

데스크톱은 `position: sticky`인 오른쪽 패널을 사용하되, 섹션 범위를 벗어나지 않게 한다. 모바일은 워드클라우드 바로 다음에 같은 상세 패널을 배치한다.

상세 패널 구성:

1. 도구명·유형·선택 학교급 순위
2. 선택 학교급 고유 학교 수와 비율
3. 초·중·고 비교 막대
4. 실제 핵심 기능
5. 관찰·해석·2학기 질문
6. 관련 도구
7. 한계 문장
8. 이용약관 확인 문구

서울시교육청 자료 링크는 에듀테크 화면의 학교급 선택기 옆에 상시 표시한다.

상세 패널은 별도의 하단 섹션으로 만들지 않는다.

### 4.6 교사개발 웹앱 카드

`TeacherWebAppCard`를 추가한다.

- 선택한 학교급 건수
- 가장 많이 나타난 개발 목적
- `자세히 보기` 버튼

상세 대화상자:

- 전체·초·중·고 분포
- 주요 개발 목적
- 익명화된 예시 카드
- 2학기 운영 인사이트

학생 제작 앱에 관한 카드, 학교명, 제외 과정 설명은 표시하지 않는다.

### 4.7 학생 사용 안내 단순화

현재의 큰 `학생 사용 전 필수 확인` 블록은 제거한다.

다음 두 요소만 유지한다.

- 도구 상세의 `사용 전 이용약관을 꼭 확인하세요`
- 서울시교육청 `AI·에듀테크 공교육 도입 및 활용 가이드라인·지원자료` 링크

도구별 연령을 하드코딩하거나 범용 AI 꼬리표를 다시 추가하지 않는다.

### 4.8 활용 유형

기존 `활용 유형` 탭도 선택한 학교급에 맞춰 갱신한다.

- 유형별 고유 학교 수와 비율
- 학교급별 두드러진 유형
- 해당 유형의 대표 도구
- 2학기 점검 질문

유형별 비율의 합은 100%가 아닐 수 있다는 설명은 접힌 `자료 읽는 법` 안에 둔다.

## 5. 디자인과 문장

### 5.1 유지할 요소

- 크림색 배경과 보라색 중심 색상
- 아이스크림·스쿱 시각 모티프
- 둥근 카드와 두꺼운 외곽선
- 큰 제목과 확대된 본문 글자
- 상단 탭 이동
- 짧은 요약을 누르면 상세 분석이 열리는 구조

### 5.2 수정할 요소

- 워드클라우드 컨테이너 높이와 내부 여백 확대
- 상세 분석을 항상 인접 위치에 배치
- 학교급 선택기의 명확한 활성 상태
- 긴 설명은 대화상자나 상세 패널에 배치
- 푸터는 작은 크기로 유지

### 5.3 문장 QA

배포 전 화면 문구를 검색해 다음 표현이 남아 있지 않은지 검사한다.

- AI선도학교
- 가운데 학교
- 롱테일
- 부진 학교
- 미집행 학교
- 가장 효과적인 도구
- 학생 제작 앱
- 범용 AI 꼬리표
- 개발·정제·스키마·분모 등 내부 작업 설명

`분모` 같은 단어는 내부 문서와 테스트에는 사용할 수 있지만 교사용 화면에는 표시하지 않는다.

## 6. 테스트

### 6.1 데이터 테스트

- 전체 학교 수 174
- 초등 86, 중등 51, 고등 37
- 학교급 합계와 전체 일치
- 같은 학교·같은 도구 중복 0건
- 모든 도구의 학교 수가 해당 학교급 전체 수 이하
- 비율 재계산 결과와 출력값 일치
- 순위 정렬 재현 가능
- 교사개발 웹앱 24·8·12·4
- 학생 제작 중심 사례 미포함
- 상용 도구 목록에 교사개발 웹앱 미포함

### 6.2 공개 데이터 검사

- 실제 학교명 0건
- 원자료 문장 0건
- 원본 CSV·HTML·JSON 미배포
- 브라우저 번들에 학교명 키가 없음
- 우리 학교 입력값을 전송하는 네트워크 코드 없음

### 6.3 기능 테스트

- 메인 진입 카드 두 개
- 예산·사례 파트의 메인으로 돌아가기
- 에듀테크 학교급 변경
- 검색과 기능 필터 조합
- 워드클라우드 페이지 이동
- 마우스·키보드·터치 도구 선택
- 상세 패널 열기·닫기
- 교사개발 웹앱 상세 열기·닫기
- 정확한 순위표와 상세 패널 연동
- 기존 예산 10분위 진단
- 기존 과제 사례 검색과 상세 보기

### 6.4 시각 테스트

다음 너비에서 직접 확인한다.

- 360px
- 768px
- 1280px
- 1440px
- 1920px

확인 항목:

- 워드클라우드 잘림·겹침 없음
- 비율 배지와 도구명 겹침 없음
- 상세 패널이 화면 밖으로 나가지 않음
- 긴 한글 문장이 비정상적으로 한 글자씩 줄바꿈되지 않음
- 학교급 버튼과 검색창 겹침 없음
- 푸터가 과도하게 크지 않음

## 7. 구현 순서

### 1단계: 데이터 확정

- 7월 24일 기준 자료 고정
- 도구 별칭 정규화
- 학교급별 고유 학교 집계
- 교사개발 웹앱 24건 검증
- 공개용 익명 데이터 생성

### 2단계: 데이터 코드 분리

- 에듀테크 타입 작성
- `edtech-data.ts` 생성
- 기존 하드코딩 데이터 제거
- 데이터 단위 테스트 추가

### 3단계: 학교급 비교 구현

- 학교급 선택기
- 학교급별 순위·비율
- 초·중·고 비교 막대
- 학교급별 인사이트

### 4단계: 워드클라우드와 상세 패널 개선

- 연속 글자 크기 적용
- 잘림 방지 배치
- 검색·필터·페이지 연동
- 데스크톱 인접 패널
- 모바일 대화상자

### 5단계: 교사개발 웹앱

- 별도 카드
- 학교급별 분포
- 익명 목적·예시
- 2학기 운영 인사이트

### 6단계: 문구와 안내 정리

- 큰 학생 사용 경고 카드 제거
- 이용약관 확인 문구 유지
- 서울시교육청 링크 유지
- 내부 설명과 낯선 용어 제거

### 7단계: 전체 회귀 검증

- 빌드와 테스트
- 예산·사례 기존 기능 확인
- 반응형과 접근성 확인
- 학교명 비노출 검사
- Vercel 미리보기 검토

### 8단계: 배포

- 검증된 변경만 커밋
- GitHub `main` 브랜치로 푸시
- Vercel Production 배포 확인
- 실제 URL에서 학교급 변경, 상세 패널, 모바일 화면 재확인

## 8. 완료 조건

다음 조건을 모두 충족하면 업데이트를 완료한 것으로 본다.

- 7월 24일 기준 초·중·고 에듀테크 집계가 적용됐다.
- 전체·초·중·고 선택이 모든 에듀테크 시각화에 일관되게 반영된다.
- 모든 도구가 워드클라우드·검색·순위표 중 하나 이상으로 접근 가능하다.
- 워드클라우드가 잘리거나 겹치지 않는다.
- 상세 분석이 페이지 맨 아래가 아니라 인접 패널 또는 대화상자에 표시된다.
- 교사개발 웹앱이 24건으로 별도 제공된다.
- 학생 제작 중심 사례가 제외됐다.
- 학교명이 공개 데이터와 빌드 결과에 없다.
- 이용약관 확인 문구와 서울시교육청 링크만 간결하게 남았다.
- 기존 예산·과제 사례 기능이 깨지지 않았다.
- Vercel Production에서 정상 동작한다.
